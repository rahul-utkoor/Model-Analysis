# Diagram Notes

The example graphs are intentionally small enough to inspect from the CLI `pretty_print()` output. This directory is reserved for future teaching diagrams derived from the same explicit IR.
